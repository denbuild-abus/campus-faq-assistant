name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # 单元测试只依赖轻量库，跳过 sentence-transformers/torch 以加快 CI
      - name: Install test dependencies
        run: |
          python -m pip install --upgrade pip
          pip install fastapi pydantic numpy rank-bm25 jieba openai python-docx pypdf python-multipart
          pip install -r requirements-dev.txt

      - name: Run tests
        run: pytest -q
